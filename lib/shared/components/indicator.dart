import 'package:flutter/cupertino.dart';

class Indictor extends StatelessWidget {
  const Indictor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("nada"),
    );
  }
}
